fn recurse(amount: int, i: int, res: int) -> int {
    if amount != i {
        return recurse(amount, i+1, res*i);
    }
    return res*i;
}

fn recurse_start(amount: int) -> int {
    return recurse(amount, 1, 1) - 1;
}

fn main(argc: int) -> int {
    return recurse_start(10);
}